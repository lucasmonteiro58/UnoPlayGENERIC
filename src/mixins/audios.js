/** Modelo de importação de áudios
 *
 */

// import { useSound } from '@vueuse/sound'

// import circle from '../assets/audios/circle.wav'

const audios = {
  setup() {
    // const audioCircle = useSound(circle)

    return {
      // audioCircle,
    }
  },
  methods: {
    // audioClickPlay() {
    //   if (this.soundState) this.audioClick.play()
    // }
  }
}

export default audios
